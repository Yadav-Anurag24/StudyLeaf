<form action="/search" method="GET" class="search-form">
    <input type="search" name="query" placeholder="Search notes..." required>
    <button type="submit"><i data-lucide="search"></i></button>
</form>

<main>
    <h2>Archive</h2>
    
    <% if (notes && notes.length > 0) { %>
        <ul class="note-list">
            <% notes.forEach(note => { %>
                <li class="note-item">
                    <a href="/note/<%= note.slug %>" class="note-title">
                        <% if (note.pinned) { %><span class="pin-badge" title="Pinned"><i data-lucide="pin"></i></span> <% } %>
                        <%= note.title %>
                        <% if (note.status === 'draft') { %><span class="draft-badge">Draft</span><% } %>
                    </a>

                    <% if (note.tags && note.tags.length > 0) { %>
                        <div class="note-tags">
                            <% note.tags.forEach(tag => { %>
                                <a href="/tags/<%= tag %>" class="tag"><%= tag %></a>
                            <% }) %>
                        </div>
                    <% } %>

                    <% if (note.date) { %>
                        <div class="note-date">
                            <%= new Date(note.date).toLocaleDateString('en-US', { year: 'numeric', month: 'long', day: 'numeric' }) %>
                            <% const words = note.content.split(/\s+/).filter(w => w.length > 0).length; %>
                            <span class="note-reading-time">· <%= Math.max(1, Math.ceil(words / 200)) %> min read</span>
                        </div>
                    <% } %>
                </li>
            <% }) %>
        </ul>

        <% if (totalPages > 1) { %>
            <nav class="pagination" aria-label="Page navigation">
                <% if (currentPage > 1) { %>
                    <a href="<%= basePath %>?page=<%= currentPage - 1 %>" class="pagination-btn pagination-prev"><i data-lucide="chevron-left"></i> Prev</a>
                <% } else { %>
                    <span class="pagination-btn pagination-prev disabled"><i data-lucide="chevron-left"></i> Prev</span>
                <% } %>

                <div class="pagination-numbers">
                    <% for (let i = 1; i <= totalPages; i++) { %>
                        <% if (i === currentPage) { %>
                            <span class="pagination-num active"><%= i %></span>
                        <% } else if (i === 1 || i === totalPages || (i >= currentPage - 1 && i <= currentPage + 1)) { %>
                            <a href="<%= basePath %>?page=<%= i %>" class="pagination-num"><%= i %></a>
                        <% } else if (i === currentPage - 2 || i === currentPage + 2) { %>
                            <span class="pagination-ellipsis">…</span>
                        <% } %>
                    <% } %>
                </div>

                <% if (currentPage < totalPages) { %>
                    <a href="<%= basePath %>?page=<%= currentPage + 1 %>" class="pagination-btn pagination-next">Next <i data-lucide="chevron-right"></i></a>
                <% } else { %>
                    <span class="pagination-btn pagination-next disabled">Next <i data-lucide="chevron-right"></i></span>
                <% } %>
            </nav>
        <% } %>

    <% } else { %>
        <p>No notes found. create one?</p>
    <% } %>
</main>

<a href="/note/new" class="fab" title="Add New Note">+</a>